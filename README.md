# Operza marketing site

The public marketing website for **Operza**, manufacturing software for Indian
factories. Operza runs the shop floor and the business books, and in Operza
Complete they are one system.

Live at **https://www.operza.in**. The product itself lives at
**https://app.operza.in** and is built in a separate repository.

> **Product truth lives in `operza-app`, not here.** Before writing or changing
> any customer-facing claim on this site, check it against
> [`kartikvagh12-bot/operza-app`](https://github.com/kartikvagh12-bot/operza-app).
> This repository markets the product; it is not a source of truth about what
> the product does.

## Status

The site is mid-refresh. Its positioning, metadata and structured data have been
brought up to date, but **most homepage section copy is still the earlier
inventory-and-production-only version** and is being rewritten in follow-up
work. Do not treat the current homepage copy as an approved description of
Operza.

## Stack

- Next.js 14 (App Router) with TypeScript
- Tailwind CSS 3
- `@supabase/supabase-js` for the lead form only
- Deployed on Vercel

Note that `operza-app` is on Next 16 / React 19 / Tailwind 4. The two share a
brand palette and typeface but not a design-token system, so components are not
portable between them.

## Routes and page architecture

| Route | Notes |
|---|---|
| `/` | Homepage. Composes the section components in `components/`. |
| `/health-check` | Manufacturing Health Check. Self-contained client assessment, no persistence, no lead gate. |
| `/sitemap.xml` | Generated by `app/sitemap.ts`. Real URLs only, no `#fragment` entries. |
| `/robots.txt` | Generated by `app/robots.ts`. |
| `/opengraph-image` | Social card, generated at build time by `next/og`. |
| `/twitter-image` | Same card, declared explicitly for the `summary_large_image` card. |

Homepage section order, as currently shipped:

```
Navbar
  Hero
  BlindSpots
  HealthCheckCTA
  Workflow
  InteractiveProductionDemo
  Features
  Screenshots
  FAQ
  FinalCTA
  Contact
Footer
FloatingAudit
```

Shared pieces:

- `lib/site.ts` is the single source for URLs, contact details and nav links.
  Every "open the app" link resolves through `SITE.app`.
- `lib/og-card.tsx` renders the social card shared by both image routes.
- `app/globals.css` holds the `@layer components` primitives
  (`.container-page`, `.btn-primary`, `.section`, `.eyebrow`, `.h-section`,
  `.p-section`, `.surface`).
- `tailwind.config.ts` carries the brand palette, which matches `operza-app`.

## Public assets

`public/` contains four files and **no product screenshots**:

| File | Purpose |
|---|---|
| `operza-logo.png` | The hexagon mark, used by the Navbar and Footer. |
| `favicon-512.png` | Favicon. |
| `apple-touch-icon.png` | iOS home-screen icon. |
| `google3ea2eded30925b65.html` | Google Search Console verification. Do not remove or rename. |

Every "product screenshot" currently on the site is hand-built Tailwind markup
inside a component, not a real capture.

## Environment variables

The contact form writes leads into a Supabase table. Two variables are needed
locally and on Vercel (Production, Preview and Development):

```
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon publishable key>
```

Copy `.env.local.example` to `.env.local` for local development.

## The `demo_requests` dependency

`components/Contact.tsx` inserts leads into the Supabase table
`demo_requests`. Two things are worth knowing:

1. The table lives in the Supabase project shared with `operza-app`
   (`kragnxmsspfaxiejyqgm`).
2. **Its schema is defined in the `Inventory_tracker` repository**
   (`supabase/migrations/003_demo_requests.sql`), which is the retired
   Streamlit app. Nothing in `operza-app` defines this table.

That is an open maintenance concern: the lead form depends on a table whose
migration lives in a repository that is being wound down. If the table is ever
dropped during that teardown, the form fails silently and the only signal is a
`console.error` in the browser. Moving the table definition into `operza-app`,
or otherwise taking ownership of it, is tracked separately and is deliberately
not part of the marketing refresh.

## Local development

```bash
npm install
cp .env.local.example .env.local   # fill in the two Supabase values
npm run dev
```

## Checks

```bash
npm run build                     # type-checks and builds
npm run lint
npm run test:marketing-foundation # static guard, see below
```

`npm run test:marketing-foundation` is a dependency-free static check over the
source. It fails if a set of known regressions comes back: a free-price offer in
the structured data, `#fragment` URLs in the sitemap, the removed Streamlit
constant, missing positioning in the metadata, or an em dash in the
customer-facing copy it covers. It reads files only. It does not build, render
or make network calls.

It deliberately covers a narrow surface. The homepage sections and the health
check still carry older copy, including em dashes, which is removed as those
surfaces are rewritten. A broader copy guard belongs with that work rather than
with a whitelist of today's debt.

### Lead-form smoke test

`.smoketest.mjs` verifies the anon-key policy on `demo_requests`: an insert
should succeed, and select, update and delete should all be refused. It inserts
one row per run.

```bash
URL=<supabase url> KEY=<anon key> node .smoketest.mjs
```

It is not wired into `package.json`, is not part of CI, and should only be run
deliberately.

## Deployment

Vercel, with the Next.js preset detected automatically. `next.config.mjs`
carries one rule: a permanent host-scoped redirect from the legacy
`operza.vercel.app` domain to `https://www.operza.in`. Production custom domains
are untouched by it.

## Copy rules

Customer-facing copy on this site follows the vocabulary shipped in
`operza-app`:

- **material**, never "part"
- **finished goods**, never "FG"
- **average cost**, never "WAC"
- **Cost changes**, never "cost intelligence"
- **workspace code**, never "slug"
- **plan**, never "suite" or "capabilities"
- no database, transaction or RPC vocabulary in anything a visitor reads

Marketing prose contains **zero em dashes** (U+2014). The only exceptions are
the empty-value `—` glyphs inside the interactive demo's tables, which are data
placeholders rather than prose.

## License

© Operza. All rights reserved.
